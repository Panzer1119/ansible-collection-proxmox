---
- name: "Ensure Ansible user '{{ proxmox_user }}' exists"
  ansible.builtin.user:
    name: "{{ proxmox_user }}"
    comment: "Ansible User"
#    generate_ssh_key: true
#    ssh_key_type: ed25519
#    ssh_key_comment: "{{ proxmox_user }}@{{ ansible_hostname }}"
    shell: /bin/bash
    create_home: true
    state: present
  become: true

#TODO Optionally set up SSH keys for the user?
...
